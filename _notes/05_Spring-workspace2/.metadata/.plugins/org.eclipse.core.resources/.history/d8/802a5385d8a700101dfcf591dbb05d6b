<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	
	<!-- 
		* 기존 마이바티스 환경설정 내용
		
		1. settings 영역 - 마이바티스 구동 시 필요한 설정값들
		2. typeAliases 영역 - VO 클래스에 대한 짧은 별칭 지정
		3. environments 영역 - 접속할 DB 정보를 지정 (이미 application.properties 파일에 기술했음)
		4. mappers 영역 - mapper 파일들의 경로를 지정 (이미 application.properties 파일에 기술했음)
		
		- 스프링 프레임워크의 IoC (제어의 역전) 특징에 의해
		  모든 주도권은 스프링이 갖기 때문에 마이바티스의 핵심 정보들을 마이바티스가 아닌 스프링한테 알려준 것
		- 마이바티스를 스프링에 연동해서 쓸 경우 mybatis-config.xml 파일 자체는 간결해짐!!
		  (중요한 설정값들은 스프링한테 넘겼으니까)
	-->
	
	<settings>
		<setting name="jdbcTypeForNull" value="NULL" />
	</settings>
	
	
	
</configuration>  




